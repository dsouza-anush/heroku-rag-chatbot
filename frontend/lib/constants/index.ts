/**
 * Centralized constants exports
 * Import from '@/lib/constants' for cleaner imports
 */

// Guest user detection pattern: guest-{timestamp}
export const guestRegex = /^guest-\d+$/;

export * from './polling';
